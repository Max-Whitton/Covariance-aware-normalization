# Modernized for Python 3.12. Original env was Python 3.8.
# torch / torchvision / torchaudio NOT listed — install those separately
# to match your CUDA (you already have torch 2.9.1 in spring-2026-pyt).

# --- scientific stack (py3.12-compatible floors) ---
numpy>=1.26
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
pillow>=10
opencv-python>=4.8

# --- video / media ---
av>=11.0            # was 8.0.3; needs bump for py3.12 build
decord==0.6.0
ffmpeg-python       # the pypi 'ffmpeg' pkg is abandoned; use ffmpeg-python

# --- HF / NLP ---
# Original pinned transformers==4.2.1 + tokenizers==0.9.4 — too old for py3.12.
# If the code uses removed transformers APIs, you'll need to patch it.
transformers>=4.30
tokenizers>=0.13
sacremoses
regex

# --- training / logging ---
timm>=0.9
tensorboardx
tqdm
humanize
dominate
click
joblib
filelock
psutil

# --- misc ---
protobuf
pyyaml
requests
packaging
python-dateutil
pytz
six
idna
urllib3
pyparsing
threadpoolctl
